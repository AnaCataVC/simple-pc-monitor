<UserControl x:Class="SystemCoreMonitor.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="950">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="12,8">
        <StackPanel MaxWidth="700" HorizontalAlignment="Left">
            <!-- Theme Selection Card -->
            <Border Style="{StaticResource BentoCardStyle}" Padding="18,14" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="Paleta de Colores y Tema" FontSize="15" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,4"/>
                    <TextBlock Text="Elige la combinación visual para el tablero y widgets" FontSize="12.5" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,12"/>

                    <WrapPanel>
                        <Button Style="{StaticResource PillActionButtonStyle}" Command="{Binding SetThemeCommand}" CommandParameter="Dark" Margin="0,0,8,6" Padding="14,8">
                            <StackPanel Orientation="Horizontal">
                                <Border Background="#1E1E2E" Width="14" Height="14" CornerRadius="7" Margin="0,0,8,0"/>
                                <TextBlock Text="Pastel Dark" FontSize="13" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Button>

                        <Button Style="{StaticResource PillActionButtonStyle}" Command="{Binding SetThemeCommand}" CommandParameter="Light" Margin="0,0,8,6" Padding="14,8">
                            <StackPanel Orientation="Horizontal">
                                <Border Background="#EFF1F5" Width="14" Height="14" CornerRadius="7" Margin="0,0,8,0"/>
                                <TextBlock Text="Pastel Light" FontSize="13" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Button>

                        <Button Style="{StaticResource PillActionButtonStyle}" Command="{Binding SetThemeCommand}" CommandParameter="Neon" Margin="0,0,8,6" Padding="14,8">
                            <StackPanel Orientation="Horizontal">
                                <Border Background="#0D0221" Width="14" Height="14" CornerRadius="7" Margin="0,0,8,0"/>
                                <TextBlock Text="Cyber Neon" FontSize="13" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Button>

                        <Button Style="{StaticResource PillActionButtonStyle}" Command="{Binding SetThemeCommand}" CommandParameter="Rose" Margin="0,0,8,6" Padding="14,8">
                            <StackPanel Orientation="Horizontal">
                                <Border Background="#FFF1F2" Width="14" Height="14" CornerRadius="7" Margin="0,0,8,0"/>
                                <TextBlock Text="Sakura Rose" FontSize="13" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Button>
                    </WrapPanel>
                </StackPanel>
            </Border>

            <!-- Language Selection Card -->
            <Border Style="{StaticResource BentoCardStyle}" Padding="18,14" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="Idioma de la Interfaz" FontSize="15" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,4"/>
                    <TextBlock Text="Configura el idioma preferido para etiquetas, tooltips e informes" FontSize="12.5" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,12"/>

                    <StackPanel Orientation="Horizontal">
                        <Button Style="{StaticResource PillActionButtonStyle}" Command="{Binding SetLanguageCommand}" CommandParameter="es" Margin="0,0,10,0" Padding="16,8">
                            <TextBlock Text="Español (ES)" FontSize="13" FontWeight="SemiBold"/>
                        </Button>
                        <Button Style="{StaticResource PillActionButtonStyle}" Command="{Binding SetLanguageCommand}" CommandParameter="en" Padding="16,8">
                            <TextBlock Text="English (EN)" FontSize="13" FontWeight="SemiBold"/>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Refresh Interval Card -->
            <Border Style="{StaticResource BentoCardStyle}" Padding="18,14" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="Frecuencia de Muestreo de Telemetría" FontSize="15" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,4"/>
                    <TextBlock Text="Intervalo de actualización en segundo plano de CPU, memoria y procesos" FontSize="12.5" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,12"/>

                    <StackPanel Orientation="Horizontal">
                        <Button Style="{StaticResource PillActionButtonStyle}" Command="{Binding SetIntervalCommand}" CommandParameter="1" Margin="0,0,8,0" Padding="14,7" Content="1 segundo"/>
                        <Button Style="{StaticResource PillActionButtonStyle}" Command="{Binding SetIntervalCommand}" CommandParameter="2" Margin="0,0,8,0" Padding="14,7" Content="2 segundos"/>
                        <Button Style="{StaticResource PillActionButtonStyle}" Command="{Binding SetIntervalCommand}" CommandParameter="3" Margin="0,0,8,0" Padding="14,7" Content="3 segundos (Recomendado)"/>
                        <Button Style="{StaticResource PillActionButtonStyle}" Command="{Binding SetIntervalCommand}" CommandParameter="5" Padding="14,7" Content="5 segundos"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>