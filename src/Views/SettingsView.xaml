<UserControl x:Class="SystemCoreMonitor.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="950">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="12,8">
        <StackPanel MaxWidth="720" HorizontalAlignment="Left">
            <!-- Theme Selection Card -->
            <Border Style="{StaticResource BentoCardStyle}" Padding="18,14" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="{Binding TitleTheme}" FontSize="15" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,4"/>
                    <TextBlock Text="{Binding DescTheme}" FontSize="12.5" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,12"/>

                    <WrapPanel>
                        <!-- Pastel Dark -->
                        <Button Command="{Binding SetThemeCommand}" CommandParameter="Dark" Margin="0,0,10,8" Padding="14,8">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource PillActionButtonStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentTheme}" Value="Dark">
                                            <Setter Property="Background" Value="{DynamicResource BgControlActive}"/>
                                            <Setter Property="BorderBrush" Value="{DynamicResource AccentCpu}"/>
                                            <Setter Property="BorderThickness" Value="1.5"/>
                                            <Setter Property="Foreground" Value="{DynamicResource AccentCpu}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <!-- Palette Preview Swatch -->
                                <Border Background="#1E1E2E" BorderBrush="#45475A" BorderThickness="1" CornerRadius="6" Padding="4,3" Margin="0,0,8,0" VerticalAlignment="Center">
                                    <StackPanel Orientation="Horizontal">
                                        <Border Background="#89B4FA" Width="10" Height="10" CornerRadius="5" Margin="0,0,3,0"/>
                                        <Border Background="#CBA6F7" Width="10" Height="10" CornerRadius="5"/>
                                    </StackPanel>
                                </Border>
                                <TextBlock Text="Pastel Dark" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <!-- Pastel Light -->
                        <Button Command="{Binding SetThemeCommand}" CommandParameter="Light" Margin="0,0,10,8" Padding="14,8">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource PillActionButtonStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentTheme}" Value="Light">
                                            <Setter Property="Background" Value="{DynamicResource BgControlActive}"/>
                                            <Setter Property="BorderBrush" Value="{DynamicResource AccentCpu}"/>
                                            <Setter Property="BorderThickness" Value="1.5"/>
                                            <Setter Property="Foreground" Value="{DynamicResource AccentCpu}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <!-- Palette Preview Swatch -->
                                <Border Background="#EFF1F5" BorderBrush="#CCD0DA" BorderThickness="1" CornerRadius="6" Padding="4,3" Margin="0,0,8,0" VerticalAlignment="Center">
                                    <StackPanel Orientation="Horizontal">
                                        <Border Background="#1E66F5" Width="10" Height="10" CornerRadius="5" Margin="0,0,3,0"/>
                                        <Border Background="#8839EF" Width="10" Height="10" CornerRadius="5"/>
                                    </StackPanel>
                                </Border>
                                <TextBlock Text="Pastel Light" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <!-- Cyber Neon -->
                        <Button Command="{Binding SetThemeCommand}" CommandParameter="Neon" Margin="0,0,10,8" Padding="14,8">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource PillActionButtonStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentTheme}" Value="Neon">
                                            <Setter Property="Background" Value="{DynamicResource BgControlActive}"/>
                                            <Setter Property="BorderBrush" Value="{DynamicResource AccentCpu}"/>
                                            <Setter Property="BorderThickness" Value="1.5"/>
                                            <Setter Property="Foreground" Value="{DynamicResource AccentCpu}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <!-- Palette Preview Swatch -->
                                <Border Background="#0D0221" BorderBrush="#FF007F" BorderThickness="1" CornerRadius="6" Padding="4,3" Margin="0,0,8,0" VerticalAlignment="Center">
                                    <StackPanel Orientation="Horizontal">
                                        <Border Background="#00F0FF" Width="10" Height="10" CornerRadius="5" Margin="0,0,3,0"/>
                                        <Border Background="#FF007F" Width="10" Height="10" CornerRadius="5"/>
                                    </StackPanel>
                                </Border>
                                <TextBlock Text="Cyber Neon" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <!-- Sakura Rose -->
                        <Button Command="{Binding SetThemeCommand}" CommandParameter="Rose" Margin="0,0,10,8" Padding="14,8">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource PillActionButtonStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentTheme}" Value="Rose">
                                            <Setter Property="Background" Value="{DynamicResource BgControlActive}"/>
                                            <Setter Property="BorderBrush" Value="{DynamicResource AccentCpu}"/>
                                            <Setter Property="BorderThickness" Value="1.5"/>
                                            <Setter Property="Foreground" Value="{DynamicResource AccentCpu}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <!-- Palette Preview Swatch -->
                                <Border Background="#FFF1F2" BorderBrush="#FECDD3" BorderThickness="1" CornerRadius="6" Padding="4,3" Margin="0,0,8,0" VerticalAlignment="Center">
                                    <StackPanel Orientation="Horizontal">
                                        <Border Background="#E11D48" Width="10" Height="10" CornerRadius="5" Margin="0,0,3,0"/>
                                        <Border Background="#FB7185" Width="10" Height="10" CornerRadius="5"/>
                                    </StackPanel>
                                </Border>
                                <TextBlock Text="Sakura Rose" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </WrapPanel>
                </StackPanel>
            </Border>

            <!-- Language Selection Card -->
            <Border Style="{StaticResource BentoCardStyle}" Padding="18,14" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="{Binding TitleLanguage}" FontSize="15" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,4"/>
                    <TextBlock Text="{Binding DescLanguage}" FontSize="12.5" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,12"/>

                    <StackPanel Orientation="Horizontal">
                        <Button Command="{Binding SetLanguageCommand}" CommandParameter="es" Margin="0,0,10,0" Padding="16,8">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource PillActionButtonStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentLanguage}" Value="es">
                                            <Setter Property="Background" Value="{DynamicResource BgControlActive}"/>
                                            <Setter Property="BorderBrush" Value="{DynamicResource AccentCpu}"/>
                                            <Setter Property="BorderThickness" Value="1.5"/>
                                            <Setter Property="Foreground" Value="{DynamicResource AccentCpu}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <TextBlock Text="Español (ES)" FontSize="13" FontWeight="SemiBold"/>
                        </Button>
                        <Button Command="{Binding SetLanguageCommand}" CommandParameter="en" Padding="16,8">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource PillActionButtonStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentLanguage}" Value="en">
                                            <Setter Property="Background" Value="{DynamicResource BgControlActive}"/>
                                            <Setter Property="BorderBrush" Value="{DynamicResource AccentCpu}"/>
                                            <Setter Property="BorderThickness" Value="1.5"/>
                                            <Setter Property="Foreground" Value="{DynamicResource AccentCpu}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <TextBlock Text="English (EN)" FontSize="13" FontWeight="SemiBold"/>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Refresh Interval Card -->
            <Border Style="{StaticResource BentoCardStyle}" Padding="18,14" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="{Binding TitleInterval}" FontSize="15" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,4"/>
                    <TextBlock Text="{Binding DescInterval}" FontSize="12.5" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,12"/>

                    <WrapPanel>
                        <!-- 1 second -->
                        <Button Command="{Binding SetIntervalCommand}" CommandParameter="1" Margin="0,0,8,6" Padding="14,7">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource PillActionButtonStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsInterval1s}" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource BgControlActive}"/>
                                            <Setter Property="BorderBrush" Value="{DynamicResource AccentCpu}"/>
                                            <Setter Property="BorderThickness" Value="1.5"/>
                                            <Setter Property="Foreground" Value="{DynamicResource AccentCpu}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <TextBlock Text="{Binding Interval1sText}" FontSize="12.5" FontWeight="SemiBold"/>
                        </Button>

                        <!-- 3 seconds (Recommended) -->
                        <Button Command="{Binding SetIntervalCommand}" CommandParameter="3" Margin="0,0,8,6" Padding="14,7">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource PillActionButtonStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsInterval3s}" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource BgControlActive}"/>
                                            <Setter Property="BorderBrush" Value="{DynamicResource AccentCpu}"/>
                                            <Setter Property="BorderThickness" Value="1.5"/>
                                            <Setter Property="Foreground" Value="{DynamicResource AccentCpu}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <TextBlock Text="{Binding Interval3sText}" FontSize="12.5" FontWeight="SemiBold"/>
                        </Button>

                        <!-- 5 seconds -->
                        <Button Command="{Binding SetIntervalCommand}" CommandParameter="5" Margin="0,0,8,6" Padding="14,7">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource PillActionButtonStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsInterval5s}" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource BgControlActive}"/>
                                            <Setter Property="BorderBrush" Value="{DynamicResource AccentCpu}"/>
                                            <Setter Property="BorderThickness" Value="1.5"/>
                                            <Setter Property="Foreground" Value="{DynamicResource AccentCpu}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <TextBlock Text="{Binding Interval5sText}" FontSize="12.5" FontWeight="SemiBold"/>
                        </Button>

                        <!-- 10 seconds -->
                        <Button Command="{Binding SetIntervalCommand}" CommandParameter="10" Margin="0,0,8,6" Padding="14,7">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource PillActionButtonStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsInterval10s}" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource BgControlActive}"/>
                                            <Setter Property="BorderBrush" Value="{DynamicResource AccentCpu}"/>
                                            <Setter Property="BorderThickness" Value="1.5"/>
                                            <Setter Property="Foreground" Value="{DynamicResource AccentCpu}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <TextBlock Text="{Binding Interval10sText}" FontSize="12.5" FontWeight="SemiBold"/>
                        </Button>
                    </WrapPanel>
                </StackPanel>
            </Border>

            <!-- Hardware Accelerators Monitoring Card -->
            <Border Style="{StaticResource BentoCardStyle}" Padding="18,14" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="{Binding TitleAccelerators}" FontSize="15" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,4"/>
                    <TextBlock Text="{Binding DescAccelerators}" FontSize="12.5" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,12"/>

                    <StackPanel Orientation="Horizontal">
                        <!-- Enabled Button -->
                        <Button Command="{Binding EnableAcceleratorsCommand}" Margin="0,0,10,0" Padding="16,8">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource PillActionButtonStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding EnableAcceleratorsMonitoring}" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource BgControlActive}"/>
                                            <Setter Property="BorderBrush" Value="{DynamicResource AccentCpu}"/>
                                            <Setter Property="BorderThickness" Value="1.5"/>
                                            <Setter Property="Foreground" Value="{DynamicResource AccentCpu}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="⚡" FontSize="13" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding AccelEnabledText}" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <!-- Disabled Button -->
                        <Button Command="{Binding DisableAcceleratorsCommand}" Padding="16,8">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource PillActionButtonStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding EnableAcceleratorsMonitoring}" Value="False">
                                            <Setter Property="Background" Value="{DynamicResource BgControlActive}"/>
                                            <Setter Property="BorderBrush" Value="{DynamicResource AccentCpu}"/>
                                            <Setter Property="BorderThickness" Value="1.5"/>
                                            <Setter Property="Foreground" Value="{DynamicResource AccentCpu}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="💤" FontSize="13" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding AccelDisabledText}" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- AI Transcript Retention Period Card -->
            <Border Style="{StaticResource BentoCardStyle}" Padding="18,14" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="{Binding TitleRetention}" FontSize="15" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,4"/>
                    <TextBlock Text="{Binding DescRetention}" FontSize="12.5" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,12"/>

                    <WrapPanel Orientation="Horizontal">
                        <!-- 3 days -->
                        <Button Command="{Binding SetRetentionCommand}" CommandParameter="3" Margin="0,0,8,6" Padding="14,7">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource PillActionButtonStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsRetention3d}" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource BgControlActive}"/>
                                            <Setter Property="BorderBrush" Value="{DynamicResource AccentCpu}"/>
                                            <Setter Property="BorderThickness" Value="1.5"/>
                                            <Setter Property="Foreground" Value="{DynamicResource AccentCpu}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <TextBlock Text="{Binding Retention3dText}" FontSize="12.5" FontWeight="SemiBold"/>
                        </Button>

                        <!-- 7 days (Recommended) -->
                        <Button Command="{Binding SetRetentionCommand}" CommandParameter="7" Margin="0,0,8,6" Padding="14,7">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource PillActionButtonStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsRetention7d}" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource BgControlActive}"/>
                                            <Setter Property="BorderBrush" Value="{DynamicResource AccentCpu}"/>
                                            <Setter Property="BorderThickness" Value="1.5"/>
                                            <Setter Property="Foreground" Value="{DynamicResource AccentCpu}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <TextBlock Text="{Binding Retention7dText}" FontSize="12.5" FontWeight="SemiBold"/>
                        </Button>

                        <!-- 14 days -->
                        <Button Command="{Binding SetRetentionCommand}" CommandParameter="14" Margin="0,0,8,6" Padding="14,7">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource PillActionButtonStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsRetention14d}" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource BgControlActive}"/>
                                            <Setter Property="BorderBrush" Value="{DynamicResource AccentCpu}"/>
                                            <Setter Property="BorderThickness" Value="1.5"/>
                                            <Setter Property="Foreground" Value="{DynamicResource AccentCpu}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <TextBlock Text="{Binding Retention14dText}" FontSize="12.5" FontWeight="SemiBold"/>
                        </Button>

                        <!-- 30 days -->
                        <Button Command="{Binding SetRetentionCommand}" CommandParameter="30" Margin="0,0,8,6" Padding="14,7">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource PillActionButtonStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsRetention30d}" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource BgControlActive}"/>
                                            <Setter Property="BorderBrush" Value="{DynamicResource AccentCpu}"/>
                                            <Setter Property="BorderThickness" Value="1.5"/>
                                            <Setter Property="Foreground" Value="{DynamicResource AccentCpu}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <TextBlock Text="{Binding Retention30dText}" FontSize="12.5" FontWeight="SemiBold"/>
                        </Button>
                    </WrapPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>