<UserControl x:Class="SystemCoreMonitor.Views.ServicesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="950">
    <Grid Margin="12,8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Services Summary Header -->
        <Border Grid.Row="0" Style="{StaticResource BentoCardStyle}" Padding="16,12" Margin="0,0,0,12">
            <DockPanel LastChildFill="False">
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" VerticalAlignment="Center">
                    <Path Data="{StaticResource IconService}" Fill="{DynamicResource AccentCpu}" Width="22" Height="22" Stretch="Uniform" Margin="0,0,12,0"/>
                    <StackPanel>
                        <TextBlock Text="Servicios Esenciales de Windows" FontSize="15" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}"/>
                        <TextBlock Text="Control y monitoreo de servicios críticos del sistema" FontSize="12.5" Foreground="{DynamicResource TextSecondary}"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" VerticalAlignment="Center">
                    <Border Background="{DynamicResource BgControlHover}" CornerRadius="6" Padding="10,6" Margin="0,0,8,0">
                        <TextBlock Text="{Binding Metric.RunningCount, StringFormat='{}En ejecución: {0}'}" FontSize="13" FontWeight="Bold" Foreground="#10B981"/>
                    </Border>
                    <Border Background="{DynamicResource BgControlHover}" CornerRadius="6" Padding="10,6">
                        <TextBlock Text="{Binding Metric.StoppedCount, StringFormat='{}Detenidos: {0}'}" FontSize="13" FontWeight="Bold" Foreground="#F59E0B"/>
                    </Border>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Services List -->
        <Border Grid.Row="1" Style="{StaticResource BentoCardStyle}" Padding="6">
            <ListView ItemsSource="{Binding Services}" Background="Transparent" BorderThickness="0">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{DynamicResource BgControl}" CornerRadius="6" Padding="12,8" Margin="0,2">
                            <DockPanel LastChildFill="True">
                                <!-- Action Buttons -->
                                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" VerticalAlignment="Center" Margin="12,0,0,0">
                                    <Button Style="{StaticResource PillActionButtonStyle}" 
                                            Command="{Binding DataContext.StartServiceCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Background="#10B981" Foreground="White" Padding="8,4" Margin="0,0,6,0">
                                        <TextBlock Text="Iniciar" FontSize="12" FontWeight="SemiBold"/>
                                    </Button>
                                    <Button Style="{StaticResource PillActionButtonStyle}" 
                                            Command="{Binding DataContext.StopServiceCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Background="#EF4444" Foreground="White" Padding="8,4" Margin="0,0,6,0">
                                        <TextBlock Text="Detener" FontSize="12" FontWeight="SemiBold"/>
                                    </Button>
                                    <Button Style="{StaticResource PillActionButtonStyle}" 
                                            Command="{Binding DataContext.RestartServiceCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Padding="8,4">
                                        <TextBlock Text="Reiniciar" FontSize="12" FontWeight="SemiBold"/>
                                    </Button>
                                </StackPanel>

                                <!-- Status Badge -->
                                <Border DockPanel.Dock="Right" Background="{DynamicResource BgControlHover}" CornerRadius="4" Padding="8,4" VerticalAlignment="Center" Margin="12,0,0,0">
                                    <TextBlock Text="{Binding Status}" FontSize="12.5" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}"/>
                                </Border>

                                <!-- Service Info -->
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="{Binding DisplayName}" FontSize="13.5" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}"/>
                                    <TextBlock Text="{Binding ServiceName}" FontSize="11.5" Foreground="{DynamicResource TextSecondary}"/>
                                </StackPanel>
                            </DockPanel>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>
    </Grid>
</UserControl>