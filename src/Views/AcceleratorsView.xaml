<UserControl x:Class="SystemCoreMonitor.Views.AcceleratorsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="950">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="12,8">
        <StackPanel>
            <!-- GPU Dedicated Card -->
            <Border Style="{StaticResource BentoCardStyle}" Padding="18,14" Margin="0,0,0,12">
                <StackPanel>
                    <DockPanel Margin="0,0,0,12">
                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                            <Path Data="{StaticResource IconGpu}" Fill="{DynamicResource AccentGpu}" Width="24" Height="24" Stretch="Uniform" Margin="0,0,12,0"/>
                            <StackPanel>
                                <TextBlock Text="{Binding Gpu.Name, FallbackValue='GPU'}" FontSize="15" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}"/>
                                <TextBlock Text="{Binding Gpu.Vendor, FallbackValue='Adaptador de Pantalla DirectX'}" FontSize="12.5" Foreground="{DynamicResource TextSecondary}"/>
                            </StackPanel>
                        </StackPanel>
                        <TextBlock DockPanel.Dock="Right" Text="{Binding Gpu.LoadPercent, StringFormat='{}Carga: {0:F0}%'}" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource AccentGpu}" VerticalAlignment="Center"/>
                    </DockPanel>

                    <!-- VRAM Progress -->
                    <DockPanel Margin="0,0,0,6">
                        <TextBlock Text="Memoria VRAM Dedicada:" FontSize="13" Foreground="{DynamicResource TextSecondary}"/>
                        <TextBlock DockPanel.Dock="Right" Text="{Binding Gpu.DedicatedVramPercent, StringFormat='{}{0:F1}%'}" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}"/>
                    </DockPanel>
                    <ProgressBar Style="{StaticResource PastelProgressBarStyle}" Value="{Binding Gpu.DedicatedVramPercent}" Foreground="{DynamicResource AccentGpu}" Height="8" Margin="0,0,0,12"/>

                    <!-- GPU Engines Breakdown Grid -->
                    <UniformGrid Columns="3" Margin="0,6,0,0">
                        <Border Background="{DynamicResource BgControl}" CornerRadius="6" Padding="10,8" Margin="0,0,6,0">
                            <StackPanel>
                                <TextBlock Text="Render 3D" FontSize="12" Foreground="{DynamicResource TextSecondary}"/>
                                <TextBlock Text="{Binding Gpu.Engines.Engine3DPercent, StringFormat='{}{0:F1}%'}" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}"/>
                            </StackPanel>
                        </Border>
                        <Border Background="{DynamicResource BgControl}" CornerRadius="6" Padding="10,8" Margin="3,0,3,0">
                            <StackPanel>
                                <TextBlock Text="Cómputo DirectML" FontSize="12" Foreground="{DynamicResource TextSecondary}"/>
                                <TextBlock Text="{Binding Gpu.Engines.ComputePercent, StringFormat='{}{0:F1}%'}" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}"/>
                            </StackPanel>
                        </Border>
                        <Border Background="{DynamicResource BgControl}" CornerRadius="6" Padding="10,8" Margin="6,0,0,0">
                            <StackPanel>
                                <TextBlock Text="Decodificación Video" FontSize="12" Foreground="{DynamicResource TextSecondary}"/>
                                <TextBlock Text="{Binding Gpu.Engines.VideoDecodePercent, StringFormat='{}{0:F1}%'}" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}"/>
                            </StackPanel>
                        </Border>
                    </UniformGrid>
                </StackPanel>
            </Border>

            <!-- NPU / Neural Processing Unit Card -->
            <Border Style="{StaticResource BentoCardStyle}" Padding="18,14">
                <StackPanel>
                    <DockPanel Margin="0,0,0,8">
                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                            <Path Data="{StaticResource IconNpu}" Fill="{DynamicResource AccentCpu}" Width="24" Height="24" Stretch="Uniform" Margin="0,0,12,0"/>
                            <StackPanel>
                                <TextBlock Text="NPU (Neural Processing Unit)" FontSize="15" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}"/>
                                <TextBlock Text="Acelerador de Inteligencia Artificial para Windows Copilot+ PC" FontSize="12.5" Foreground="{DynamicResource TextSecondary}"/>
                            </StackPanel>
                        </StackPanel>
                        <Border DockPanel.Dock="Right" Background="{DynamicResource BgControlHover}" CornerRadius="6" Padding="10,5">
                            <TextBlock Text="{Binding Npu.Status}" FontSize="12.5" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}"/>
                        </Border>
                    </DockPanel>

                    <TextBlock Text="{Binding Npu.Name}" FontSize="13" Foreground="{DynamicResource TextPrimary}" Margin="36,4,0,0"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>