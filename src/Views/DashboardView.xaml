<UserControl x:Class="SystemCoreMonitor.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:conv="clr-namespace:SystemCoreMonitor.UI.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="950">
    <UserControl.Resources>
        <conv:BytesToHumanReadableConverter x:Key="BytesConverter"/>
        <conv:ThresholdToBrushConverter x:Key="ThresholdConverter"/>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="12,8">
        <StackPanel>
            <!-- Top Action Ribbon with Larger Icons & Typography -->
            <Border Style="{StaticResource BentoCardStyle}" Padding="14,10" Margin="0,0,0,12">
                <DockPanel LastChildFill="False">
                    <!-- Quick Clean Buttons -->
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                        <Button Style="{StaticResource PillActionButtonStyle}" Command="{Binding OptimizeMemoryCommand}" Margin="0,0,8,0" Padding="12,6">
                            <StackPanel Orientation="Horizontal">
                                <Path Data="{StaticResource IconBroom}" Fill="{DynamicResource AccentMem}" Width="16" Height="16" Stretch="Uniform" Margin="0,0,6,0"/>
                                <TextBlock Text="Optimizar RAM" FontWeight="SemiBold" FontSize="12.5"/>
                            </StackPanel>
                        </Button>

                        <Button Style="{StaticResource PillActionButtonStyle}" Command="{Binding CleanTempCommand}" Margin="0,0,8,0" Padding="12,6">
                            <StackPanel Orientation="Horizontal">
                                <Path Data="{StaticResource IconBroom}" Fill="{DynamicResource AccentDisk}" Width="16" Height="16" Stretch="Uniform" Margin="0,0,6,0"/>
                                <TextBlock Text="Limpiar Temp" FontWeight="SemiBold" FontSize="12.5"/>
                            </StackPanel>
                        </Button>

                        <Button Style="{StaticResource PillActionButtonStyle}" Command="{Binding LaunchTaskMgrCommand}" Margin="0,0,8,0" Padding="12,6">
                            <StackPanel Orientation="Horizontal">
                                <Path Data="{StaticResource IconProcess}" Fill="{DynamicResource TextSecondary}" Width="16" Height="16" Stretch="Uniform" Margin="0,0,6,0"/>
                                <TextBlock Text="Task Manager" FontWeight="SemiBold" FontSize="12.5"/>
                            </StackPanel>
                        </Button>

                        <Button Style="{StaticResource PillActionButtonStyle}" Command="{Binding LaunchResMonCommand}" Margin="0,0,8,0" Padding="12,6">
                            <StackPanel Orientation="Horizontal">
                                <Path Data="{StaticResource IconLaunch}" Fill="{DynamicResource TextSecondary}" Width="16" Height="16" Stretch="Uniform" Margin="0,0,6,0"/>
                                <TextBlock Text="Resource Monitor" FontWeight="SemiBold" FontSize="12.5"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>

                    <!-- Power Scheme Segmented Control -->
                    <Border Background="{DynamicResource BgSurface}" CornerRadius="7" Padding="2" VerticalAlignment="Center" DockPanel.Dock="Right" BorderBrush="{DynamicResource BorderSubtle}" BorderThickness="1">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="⚡ Plan:" Foreground="{DynamicResource TextSecondary}" FontSize="12" VerticalAlignment="Center" Margin="6,0,4,0"/>
                            <Button Command="{Binding SwitchPowerPlanCommand}" CommandParameter="Saver" Content="Eco" Padding="10,4">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource SegmentedPillButtonStyle}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding CurrentPowerPlan}" Value="Saver">
                                                <Setter Property="Background" Value="{DynamicResource BgControlActive}"/>
                                                <Setter Property="Foreground" Value="{DynamicResource AccentCpu}"/>
                                                <Setter Property="FontWeight" Value="Bold"/>
                                                <Setter Property="BorderThickness" Value="1"/>
                                                <Setter Property="BorderBrush" Value="{DynamicResource AccentCpuAlpha}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                            <Button Command="{Binding SwitchPowerPlanCommand}" CommandParameter="Balanced" Content="Equilibrado" Padding="10,4">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource SegmentedPillButtonStyle}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding CurrentPowerPlan}" Value="Balanced">
                                                <Setter Property="Background" Value="{DynamicResource BgControlActive}"/>
                                                <Setter Property="Foreground" Value="{DynamicResource AccentCpu}"/>
                                                <Setter Property="FontWeight" Value="Bold"/>
                                                <Setter Property="BorderThickness" Value="1"/>
                                                <Setter Property="BorderBrush" Value="{DynamicResource AccentCpuAlpha}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                            <Button Command="{Binding SwitchPowerPlanCommand}" CommandParameter="HighPerf" Content="Turbo" Padding="10,4">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource SegmentedPillButtonStyle}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding CurrentPowerPlan}" Value="HighPerf">
                                                <Setter Property="Background" Value="{DynamicResource BgControlActive}"/>
                                                <Setter Property="Foreground" Value="{DynamicResource AccentCpu}"/>
                                                <Setter Property="FontWeight" Value="Bold"/>
                                                <Setter Property="BorderThickness" Value="1"/>
                                                <Setter Property="BorderBrush" Value="{DynamicResource AccentCpuAlpha}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </StackPanel>
                    </Border>
                </DockPanel>
            </Border>

            <!-- Bento Grid 2x2: Hardware Telemetry HUD -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- CPU Card -->
                <Border Grid.Row="0" Grid.Column="0" Style="{StaticResource BentoCardStyle}" Margin="0,0,6,12" Padding="18,14">
                    <StackPanel>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                <Path Data="{StaticResource IconCpu}" Fill="{DynamicResource AccentCpu}" Width="22" Height="22" Stretch="Uniform" Margin="0,0,10,0"/>
                                <TextBlock Text="PROCESADOR (CPU)" FontWeight="Bold" FontSize="14" Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Grid.Column="1" Text="{Binding Cpu.LoadDisplay}" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource AccentCpu}" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="10,0,0,0"/>
                        </Grid>

                        <!-- Progress Bar -->
                        <ProgressBar Style="{StaticResource PastelProgressBarStyle}" Value="{Binding Cpu.LoadPercent}" Foreground="{DynamicResource AccentCpu}" Height="8" Margin="0,0,0,10"/>

                        <DockPanel LastChildFill="False">
                            <TextBlock Text="{Binding Cpu.CoreCountDisplay, FallbackValue='Núcleos lógicos activos'}" Foreground="{DynamicResource TextSecondary}" FontSize="12"/>
                            <TextBlock DockPanel.Dock="Right" Text="{Binding Cpu.Status}" Foreground="{DynamicResource TextSecondary}" FontSize="12"/>
                        </DockPanel>
                    </StackPanel>
                </Border>

                <!-- RAM Memory Card -->
                <Border Grid.Row="0" Grid.Column="1" Style="{StaticResource BentoCardStyle}" Margin="6,0,0,12" Padding="18,14">
                    <StackPanel>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                <Path Data="{StaticResource IconRam}" Fill="{DynamicResource AccentRam}" Width="22" Height="22" Stretch="Uniform" Margin="0,0,10,0"/>
                                <TextBlock Text="MEMORIA (RAM)" FontWeight="Bold" FontSize="14" Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Grid.Column="1" Text="{Binding Memory.MemoryDisplay}" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource AccentRam}" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="10,0,0,0"/>
                        </Grid>

                        <!-- Progress Bar -->
                        <ProgressBar Style="{StaticResource PastelProgressBarStyle}" Value="{Binding Memory.LoadPercent}" Foreground="{DynamicResource AccentRam}" Height="8" Margin="0,0,0,10"/>

                        <DockPanel LastChildFill="False">
                            <TextBlock Text="{Binding Memory.UsageBreakdown, FallbackValue='Uso en tiempo real'}" Foreground="{DynamicResource TextSecondary}" FontSize="12"/>
                            <TextBlock DockPanel.Dock="Right" Text="{Binding Memory.Status}" Foreground="{DynamicResource TextSecondary}" FontSize="12"/>
                        </DockPanel>
                    </StackPanel>
                </Border>

                <!-- GPU Graphics Card -->
                <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource BentoCardStyle}" Margin="0,0,6,0" Padding="18,14">
                    <StackPanel>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                <Path Data="{StaticResource IconGpu}" Fill="{DynamicResource AccentGpu}" Width="22" Height="22" Stretch="Uniform" Margin="0,0,10,0"/>
                                <TextBlock Text="GRÁFICOS (GPU)" FontWeight="Bold" FontSize="14" Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Grid.Column="1" Text="{Binding Gpu.LoadPercent, StringFormat='{}{0:F0}%'}" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource AccentGpu}" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="10,0,0,0"/>
                        </Grid>

                        <!-- Progress Bar -->
                        <ProgressBar Style="{StaticResource PastelProgressBarStyle}" Value="{Binding Gpu.LoadPercent}" Foreground="{DynamicResource AccentGpu}" Height="8" Margin="0,0,0,10"/>

                        <DockPanel LastChildFill="False">
                            <TextBlock Text="{Binding Gpu.Name}" Foreground="{DynamicResource TextSecondary}" FontSize="12" TextTrimming="CharacterEllipsis" MaxWidth="320"/>
                            <TextBlock DockPanel.Dock="Right" Text="{Binding Gpu.Status}" Foreground="{DynamicResource TextSecondary}" FontSize="12"/>
                        </DockPanel>
                    </StackPanel>
                </Border>

                <!-- Network / Latency Card -->
                <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource BentoCardStyle}" Margin="6,0,0,0" Padding="18,14">
                    <StackPanel>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                <Path Data="{StaticResource IconNetwork}" Fill="{DynamicResource AccentNetwork}" Width="22" Height="22" Stretch="Uniform" Margin="0,0,10,0"/>
                                <TextBlock Text="RED Y LATENCIA" FontWeight="Bold" FontSize="14" Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Grid.Column="1" Text="{Binding Network.PingDisplay}" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource AccentNetwork}" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="10,0,0,0"/>
                        </Grid>

                        <DockPanel LastChildFill="False" Margin="0,6,0,0">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="⬇ " Foreground="{DynamicResource AccentNetwork}" FontSize="13"/>
                                <TextBlock Text="{Binding Network.DownloadDisplay}" Foreground="{DynamicResource TextPrimary}" FontSize="13" Margin="0,0,14,0"/>
                                <TextBlock Text="⬆ " Foreground="{DynamicResource AccentNetwork}" FontSize="13"/>
                                <TextBlock Text="{Binding Network.UploadDisplay}" Foreground="{DynamicResource TextPrimary}" FontSize="13"/>
                            </StackPanel>
                            <Button DockPanel.Dock="Right" Style="{StaticResource PillActionButtonStyle}" Command="{Binding FlushDnsCommand}" Padding="8,3" Margin="8,0,0,0" VerticalAlignment="Center" ToolTip="Vaciar caché del solucionador DNS de Windows (DnsFlushResolverCache)">
                                <StackPanel Orientation="Horizontal">
                                    <Path Data="{StaticResource IconNetwork}" Fill="{DynamicResource AccentNetwork}" Width="11" Height="11" Stretch="Uniform" Margin="0,0,5,0"/>
                                    <TextBlock Text="Vaciar DNS" FontWeight="SemiBold" FontSize="11"/>
                                </StackPanel>
                            </Button>
                            <TextBlock DockPanel.Dock="Right" Text="{Binding Network.AdapterName}" Foreground="{DynamicResource TextSecondary}" FontSize="12" TextTrimming="CharacterEllipsis" MaxWidth="150" VerticalAlignment="Center"/>
                        </DockPanel>
                    </StackPanel>
                </Border>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</UserControl>