<UserControl x:Class="SystemCoreMonitor.Views.StorageView"
             xmlns:conv="clr-namespace:SystemCoreMonitor.UI.Converters"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="950">
    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisConverter"/>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="12,8">
        <StackPanel>
            <!-- Storage Action Bar -->
            <Border Style="{StaticResource BentoCardStyle}" Padding="14,10" Margin="0,0,0,12">
                <DockPanel LastChildFill="False">
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                        <Button Style="{StaticResource PillActionButtonStyle}" Command="{Binding CleanTempCommand}" Margin="0,0,8,0" Padding="12,6">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🧹" FontSize="14" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <TextBlock Text="Limpiar Temporales" FontWeight="SemiBold" FontSize="12.5"/>
                            </StackPanel>
                        </Button>

                        <Button Style="{StaticResource PillActionButtonStyle}" Command="{Binding EmptyRecycleBinCommand}" Margin="0,0,8,0" Padding="12,6">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🗑️" FontSize="14" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <TextBlock Text="Vaciar Papelera" FontWeight="SemiBold" FontSize="12.5"/>
                            </StackPanel>
                        </Button>

                        <Button Style="{StaticResource PillActionButtonStyle}" Command="{Binding ScanBloatCommand}" Padding="12,6">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🔍" FontSize="13" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <TextBlock Text="Analizar Cachés" FontWeight="SemiBold" FontSize="12.5"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </DockPanel>
            </Border>

            <!-- Drives Cards Grid -->
            <TextBlock Text="Unidades de Disco" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" Margin="4,0,0,8"/>
            <ItemsControl ItemsSource="{Binding Drives}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource BentoCardStyle}" Padding="16,12" Margin="0,0,0,10">
                            <StackPanel>
                                <DockPanel Margin="0,0,0,8">
                                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                                        <Path Data="{StaticResource IconDisk}" Fill="{DynamicResource AccentDisk}" Width="20" Height="20" Stretch="Uniform" Margin="0,0,10,0"/>
                                        <TextBlock Text="{Binding Name}" FontSize="15" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}"/>
                                        <TextBlock Text="{Binding VolumeLabel, StringFormat='{} ({0})'}" FontSize="13" Foreground="{DynamicResource TextSecondary}" Margin="4,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                    <TextBlock DockPanel.Dock="Right" Text="{Binding PercentUsed, StringFormat='{}{0:F0}% usado'}" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource AccentDisk}"/>
                                </DockPanel>

                                <ProgressBar Style="{StaticResource PastelProgressBarStyle}" Value="{Binding PercentUsed}" Foreground="{DynamicResource AccentDisk}" Height="8" Margin="0,0,0,8"/>

                                <DockPanel LastChildFill="False">
                                    <TextBlock Text="{Binding UsedGB, StringFormat='{}Usado: {0:F1} GB'}" FontSize="12.5" Foreground="{DynamicResource TextSecondary}"/>
                                    <TextBlock DockPanel.Dock="Right" Text="{Binding TotalGB, StringFormat='{}Capacidad Total: {0:F1} GB'}" FontSize="12.5" Foreground="{DynamicResource TextSecondary}"/>
                                </DockPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Bloat & Cache Cleaners -->
            <TextBlock Text="Cachés Regenerables &amp; Archivos Voluminosos" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" Margin="4,8,0,8"/>
            <ItemsControl ItemsSource="{Binding BloatFindings}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource BentoCardStyle}" Padding="14,10" Margin="0,0,0,8">
                            <DockPanel LastChildFill="True">
                                <Button DockPanel.Dock="Right" Style="{StaticResource PillActionButtonStyle}" 
                                        Command="{Binding DataContext.ExecuteBloatActionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Background="{DynamicResource AccentDisk}" Foreground="White" Padding="10,5"
                                        Visibility="{Binding HasAction, Converter={StaticResource BoolToVisConverter}}">
                                    <TextBlock Text="{Binding ActionLabel, FallbackValue='Limpiar'}" FontSize="12" FontWeight="SemiBold"/>
                                </Button>

                                <StackPanel VerticalAlignment="Center">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Title}" FontSize="13.5" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}"/>
                                        <TextBlock Text="{Binding SizeDisplay, StringFormat='{} - {0}'}" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource AccentDisk}" Margin="6,0,0,0"/>
                                    </StackPanel>
                                    <TextBlock Text="{Binding ActionHint}" FontSize="12" Foreground="{DynamicResource TextSecondary}" Margin="0,2,0,0"/>
                                </StackPanel>
                            </DockPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>